{
    "name": "daikon/read-model",
    "type": "library",
    "description": "Read-model components for Daikon projects.",
    "keywords": [ "read-model", "projection", "repository", "immutable" ],
    "homepage": "https://github.com/daikon-cqrs/read-model",
    "license": "MPL-2.0",
    "prefer-stable": true,
    "minimum-stability": "dev",
    "require": {
        "php": "^7.2",
        "beberlei/assert": "^3.0",
        "daikon/data-structure": "master@dev",
        "daikon/event-sourcing": "master@dev",
        "daikon/interop": "master@dev",
        "daikon/message-bus": "master@dev",
        "daikon/metadata": "master@dev"
    },
    "require-dev": {
        "phpunit/phpunit": "^8.0",
        "sebastian/phpcpd": "^4.1",
        "squizlabs/php_codesniffer": "^3.3",
        "vimeo/psalm": "^2.0"
    },
    "autoload": {
        "psr-4": { "Daikon\\ReadModel\\" : "src" }
    },
    "autoload-dev": {
        "psr-4": { "Daikon\\Tests\\ReadModel\\": "tests" }
    },
    "archive": {
        "exclude": [
            "/build",
            "/vendor"
        ]
    },
    "scripts": {
        "build": [ "@sniff", "@tests" ],
        "code-sniffer": "phpcs -p -s --extensions=php --standard='PSR1,PSR2' src/",
        "code-sniffer-autofix": "phpcbf --standard=PSR2 -d tabWidth=4 src/",
        "copy-paste-detector": "phpcpd src/ || true",
        "sniff": [
            "@code-sniffer",
            "@copy-paste-detector",
            "@validate-json",
            "psalm"
        ],
        "test": "phpunit --no-coverage",
        "tests": "phpunit",
        "validate-json": "composer validate --no-check-all --no-check-lock",
        "validate-deps": "composer validate --with-dependencies --strict"
    }
}
